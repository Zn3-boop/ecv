# AI Desktop Agent — 后端核心依赖

# Web 框架
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0

# 数据
python-dotenv>=1.0,<2.0
pydantic>=2.7,<3.0

# HTTP 客户端
httpx>=0.27,<1.0
openai>=1.58.0

# 工具
python-multipart>=0.0.9,<1.0
python-docx>=1.1,<2.0

# 本地 Whisper STT（Faster-Whisper 本地语音识别）
faster-whisper>=1.0,<2.0
soundfile>=0.12,<1.0

# 注意：PostgreSQL 驱动已移除，项目使用 SQLite（内置）
# 如需 PostgreSQL：pip install psycopg2-binary>=2.9.9